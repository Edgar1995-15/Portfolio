on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Deploy to Netlify
      uses: netlify-action/build-and-deploy@v1
      with:
        site_id: ${{ secrets.NETLIFY_SITE_ID }}
        auth_token: ${{ secrets.AUTH_TOKEN }}
